package legacy.hedge;

import static org.fest.assertions.Assertions.*;

import org.junit.*;

public class HedgingPositionManagerTest {
  @Test
  public void should_always_succeed() {
    assertThat(42 == 42).isTrue();
  }
}
